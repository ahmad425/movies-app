project: "movies"
image:
  repository: 303437664068.dkr.ecr.us-east-1.amazonaws.com/movies-app
  tag: latest
  pullPolicy: IfNotPresent

deployment: 
  server:
    replicas: 
      replicaCount: 2
    args:
      - bundle
      - exec
      - puma
      - -c
      - "config/puma.rb"
    resources:
      requests:
        memory: 256Mi
        cpu: 250m
      limits:
        memory: 512Mi
        cpu: 500m
    service:
      type: ClusterIP
      port: 3000